@model IEnumerable<AlphaABSV2.Models.BookingStatus>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BookingStatusText)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BookingStatusText)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BookingStatusID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BookingStatusID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BookingStatusID })
        </td>
    </tr>
}

</table>

@model AlphaABSV2.Models.EmailTemplates

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var content = Model.TemplateContent;
}

<div class="content animate-panel">
    <div>
        <div class="row">
            <div class="col-lg-12">
                <div class="hpanel">
                    <div class="panel-heading">
                        <div class="panel-tools">
                            <a class="showhide"><i class="fa fa-chevron-up"></i></a>
                        </div>
                    </div>
                    <div class="panel-body">
                        <div class="summernote">
                        </div>
                    </div>
                    @Html.ActionLink("Back to Templates", "Index", null, new { @class = "btn btn-info" })

                </div>
            </div>
        </div>
        <p>
            @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-primary btn-md" })
        </p>
    </div>
</div>


@section Styles {
    @Styles.Render("~/bundles/summernote/css")
}

@section Scripts {
    @Scripts.Render("~/bundles/summernote/js")

    <script type="text/javascript">
    var piss = "";

    $(function () {

        // Initialize summernote plugin
        $('.summernote').summernote();

        //get the existing html from the model
        var content = @Html.Raw("'"+content+"'");

        //get the tempates id for finding it in the action controller
        var tempID = @Model.EmailTemplatesID.ToString();

            //inittilaise the summernote editore with the existing html
            $('.summernote').code(content);
            var sHTML = $('.summernote').code();

            $('.summernote1').summernote({
                toolbar: [
                    ['headline', ['style']],
                    ['style', ['bold', 'italic', 'underline', 'superscript', 'subscript', 'strikethrough', 'clear']],
                    ['textsize', ['fontsize']],
                    ['alignment', ['ul', 'ol', 'paragraph', 'lineheight']],
                ]
            });

           
        });

  </script>
}

@model IEnumerable<AlphaABSV2.Models.OfficeRef>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OfficeRefPeople)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OfficeRefPeople)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OfficeRefID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OfficeRefID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OfficeRefID })
        </td>
    </tr>
}

</table>
